# Integration Summary: File Storage, Authentication, Middleware, RBAC

References:

- [backend/src/controllers/profileController.js](backend/src/controllers/profileController.js)
- [backend/src/utils/awsS3.js](backend/src/utils/awsS3.js)
- [backend/src/routes/oauthRoutes.js](backend/src/routes/oauthRoutes.js)
- [backend/src/middleware/auth.js](backend/src/middleware/auth.js)
- [backend/src/routes/profileRoutes.js](backend/src/routes/profileRoutes.js)
- [backend/src/routes/userRoutes.js](backend/src/routes/userRoutes.js)

| Question / Item                    | Current State (in repo)                                                                                                                                                                                                                                       | Planned / Notes                                                                                                                                                                                              |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| File Storage                       | Implemented — resumes stored in AWS S3 via AWS SDK v3 (see utils/awsS3.js).                                                                                                                                                                                   | Continue using S3 with IAM role-based credentials; use presigned URLs for reads; keep private ACL.                                                                                                           |
| File Upload / Retrieval            | Implemented for resumes: `/api/v1/profile/resume` POST (upload), GET (presigned URL), DELETE (remove). Uses `multer` (memoryStorage) and S3 Put/Get/Delete (see profileController).                                                                           | Add UI upload flow, server-side content-type checks (already present), optional direct-browser uploads using presigned PUT URLs if desired.                                                                  |
| User Authentication                | Implemented — Google OAuth 2.0 via Passport (`/auth/google`, `/auth/google/callback`) plus legacy endpoint `/api/v1/auth/google` for ID token verification.                                                                                                   | Keep Passport flow; surface `/config` for client ID to frontend. Consider deprecating legacy GSI POST endpoint once GSI completely replaced.                                                                 |
| Authentication Tokens              | Implemented — JWTs issued during OAuth callback and legacy ID-token flow; tokens stored in session/cookie for redirect, and APIs expect Bearer JWT in `Authorization` header (middleware verifies with `JWT_SECRET`).                                         | Issue short-lived JWTs, refresh strategy if needed, store only as httpOnly cookies where frontend can't read if stricter security required.                                                                  |
| Middleware Validation              | Implemented — `protect` middleware verifies JWT, loads user, checks `isActive`/`isLocked`; global error handler present; helmet, rate-limit, session, CORS configured.                                                                                        | Keep `protect` for API endpoints; add more granular input validation and request-size/content checks for upload routes.                                                                                      |
| Role-Based Access Control (RBAC)   | Implemented — `authorize(...roles)` middleware used across routes (admin/employer/jobseeker). Examples: admin-only user listing, employer/admin job create/update/delete.                                                                                     | Maintain role checks; document role-permission matrix and apply to any new admin/employer routes.                                                                                                            |
| Which provider will you use?       | AWS S3 (configured in `RESUME_S3_BUCKET` / utils/awsS3.js).                                                                                                                                                                                                   | Use same S3 bucket; prefer IAM role auth in production.                                                                                                                                                      |
| What file types will you store?    | Resumes: PDF, DOC, DOCX (explicitly allowed). Profile photos currently kept from Google profile as URLs (not uploaded to S3).                                                                                                                                 | Primary: resumes (pdf/doc/docx). Optionally support profile images in S3 (small images) if needed.                                                                                                           |
| How will files be structured?      | Per-user keys: `resumes/${userId}/resume${ext}` (controller uses fixed per-user key to overwrite).                                                                                                                                                            | Continue per-user folder structure (e.g., `resumes/<userId>/...`) for easy cleanup and ACL mapping.                                                                                                          |
| Routes to implement (storage)      | Implemented: `POST /api/v1/profile/resume`, `GET /api/v1/profile/resume`, `PUT /api/v1/profile/resume`, `DELETE /api/v1/profile/resume` (see profileRoutes).                                                                                                  | Optionally add `GET /public/resumes/:key` with signed redirects or a presigned-URL generation endpoint for authorized viewers.                                                                               |
| Validation rules (files)           | Enforced in controller: `multer` memoryStorage with `limits: { fileSize: 10MB }`; allowed extensions `['.pdf','.doc','.docx']`; ContentType passed to S3.                                                                                                     | Keep 10MB limit or reduce for UX; scan for malware in production; return 400 for invalid types/sizes.                                                                                                        |
| How will users log in?             | Google OAuth 2.0 redirect flow via Passport (`/auth/google` → `/auth/google/callback`). Legacy POST `/api/v1/auth/google` verifies client-side GSI ID tokens.                                                                                                 | Use Passport-based OAuth for primary flow; keep `/config` endpoint to provide client ID to frontend.                                                                                                         |
| What user data will you retrieve?  | From Google ID token / profile: `sub` (Google id), `email`, `name`, `picture`, `locale`, `email_verified`. The app stores `googleProfile` and creates/updates `User`/`UserProfile`.                                                                           | Retrieve name, email, picture, locale; store minimal profile fields and map role if provided at login.                                                                                                       |
| Token management plan              | JWTs minted with `JWT_SECRET` (env), expiration via `JWT_EXPIRE` (default `7d`); server `protect` middleware verifies JWT; session used to keep token during OAuth redirect; cookies set (`token`, not httpOnly currently).                                   | Consider setting `token` as httpOnly cookie + CSRF protection; offer refresh token or shorter JWT lifetime if needed.                                                                                        |
| Routes to implement (auth)         | Present: `/auth/google`, `/auth/google/callback`, `/auth/logout`, `/auth/status`, `/config`, legacy `/api/v1/auth/google` (POST).                                                                                                                             | Document flows in frontend: redirect to `/auth/google?role=...&redirect=...`, read token from redirect or cookie, use `Authorization: Bearer <token>` for API calls.                                         |
| Middleware function details        | `protect` (middleware/auth.js): extracts Bearer token from `Authorization`, verifies via `jwt.verify`, loads `User`, checks `isActive`/`isLocked`. `authorize(...roles)` returns 403 if role mismatch.                                                        | Keep centralized `protect` and `authorize`; extend logging and more specific error messages as needed.                                                                                                       |
| Routes requiring middleware        | `router.use(protect)` applied in many routers — e.g., `profileRoutes` requires `protect`; `userRoutes` uses `authorize('admin')` for GET/all and DELETE; job/company/application routes use `authorize` for employer/admin/jobseeker-specific actions.        | Continue requiring `Authorization: Bearer <token>` for protected APIs; ensure frontend sends token in header.                                                                                                |
| Error handling plan                | Global `errorHandler` middleware exists; `protect` returns 401 for missing/invalid tokens; `authorize` returns 403 for role denial. Controllers throw `ApiError` with status and message.                                                                     | Ensure consistent API responses for 401/403/400/500; document in API docs.                                                                                                                                   |
| User roles                         | Observed roles: `jobseeker`, `employer`, `admin`, `rso` (seen in Swagger enums).                                                                                                                                                                              | Keep these roles; document their permissions and onboarding flows.                                                                                                                                           |
| Role-based permissions             | Implemented mappings: - `admin`: manage users, verify companies, view admin stats. - `employer`: create/update/delete jobs, manage companies. - `jobseeker`: apply/withdraw applications, view own applications.                                              | Maintain least-privilege access; add RBAC tests to verify restrictions.                                                                                                                                      |
| Routes requiring role restrictions | Examples: `GET /api/v1/users` (admin), `POST /api/v1/jobs` (employer/admin), `POST /api/v1/companies` (employer/admin), application routes: `GET /api/v1/applications/me` (jobseeker), employer endpoints for notes/status (employer/admin). See route files. | Audit all routes to ensure `authorize()` is applied where required.                                                                                                                                          |
| Testing plan (how to validate)     | Repo includes Swagger docs (`/api-docs`) and tests folder; suggested tests: use Postman / automated tests to upload sample resume, run OAuth flow (manual/CI), hit protected endpoints with/without token, verify RBAC denies/permits.                        | Use Postman collection in `backend/postman/` to exercise: resume upload (multipart/form-data), GET resume URL, auth flows, `protect` behavior (missing/invalid token → 401), role-based access checks (403). |

---

Next steps (optional):

- Create a short Postman collection with example requests for resume upload and protected endpoints.
- Add `backend/ENVIRONMENT.md` snippet listing required env vars: `RESUME_S3_BUCKET`, `AWS_REGION`, `AWS_ROLE_ARN` (optional), `GOOGLE_CLIENT_ID`, `JWT_SECRET`, `SESSION_SECRET`.

## Security & Error Handling Checklist

| Section                  | Key Questions                                                                              | Action Items                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------ | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Existing Features Review | What features in your app could interact with error handling and security measures?        | - **Features:** `CRUD` routes (jobs, companies, users), resume upload (resumes → S3, `backend/src/controllers/profileController.js`), Google OAuth/JWT (`/auth/*`, `backend/src/config/passport`), session/cookies, RBAC (`protect`/`authorize`, `backend/src/middleware/auth.js`). - **Security touchpoints:** file upload parsing, JWT verification, session cookies, CORS, CSP, public static files. Audit these files/routes and document expected error flows.                                                                                                                                                                                                     |
| Error Handling           | What types of errors will your app need to handle (e.g., 401 Unauthorized, 404 Not Found)? | - **Errors to handle:** 400 validation, 401 missing/invalid token, 403 role denied, 404 not found, 413 payload too large, 415 unsupported media type, 422 business rules, 500 internal, external service errors (S3, OAuth), rate-limit. - **Action:** Standardize error shape (e.g., {"success":false, "error": {"message","code"}}), expand `middleware/errorHandler.js` to map known errors to status codes, add structured logging for errors and include request context (no secrets).                                                                                                                                                                             |
| Security with Helmet.js  | Which Helmet.js protections will you configure, and for which routes?                      | - **Already configured:** global `helmet()` with CSP and COP in `backend/src/app.js`. - **Enable/verify:** CSP (tighten: remove `'unsafe-inline'` where possible; use nonces), HSTS in production, `X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options`/frame-ancestors, `Cross-Origin-Opener-Policy`/`Cross-Origin-Resource-Policy`. - **Scope:** apply globally for all web/API routes; tighten headers for pages serving HTML and allow more relaxed policies for local dev. Document CSP exceptions for CDNs and Google OAuth.                                                                                                                            |
| Input Validation         | What inputs are most critical to validate, and what tools will you use?                    | - **Critical inputs:** multipart file uploads (type/size), auth tokens, user/profile payloads, job/company create/update, application submissions, query params and IDs. - **Tools & actions:** `express-validator` or `Joi` for route schemas, multer limits for uploads, server-side MIME/content-type checks in `profileController.js`, `express-mongo-sanitize` to prevent injection, reject invalid payloads with 400 + descriptive message.                                                                                                                                                                                                                       |
| Testing for Edge Cases   | What edge cases will you test (invalid tokens, unauthorized access, malformed input)?      | - **Edge cases:** expired/invalid JWT, missing token, role mismatch, oversized/malformed upload, wrong file extension vs content, S3 intermittent error, partial upload, rate-limit triggers, CSP blocked resources, session fixation. - **Test plan:** Postman collection + automated tests using `supertest` and mocked S3 (`aws-sdk-client-mock`), unit tests for `protect`/`authorize`, integration tests for resume upload/download and for OAuth redirect callback error paths.                                                                                                                                                                                   |
| Implementation Workflow  | What is the step-by-step order for implementing error handling and security?               | 1. Audit routes for `protect`/`authorize` and add missing checks. 2. Centralize error types and strengthen `middleware/errorHandler.js`. 3. Add input validation middleware (`express-validator`/Joi) on critical routes. 4. Harden `helmet()` settings in `backend/src/app.js` (enable HSTS in prod, tighten CSP, remove `'unsafe-inline'` with nonces). 5. Enforce multer limits & server-side file checks in `profileController.js`. 6. Add `express-mongo-sanitize` and confirm CORS settings. 7. Add/extend logging and rate-limiting; monitor S3/OAuth failures. 8. Create Postman collection and automated tests; run in CI and stage before production rollout. |
