// seedAbout.js - deprecated
// This project no longer uses an automatic seeding script. Data should be
// imported manually (Postman / admin UI) using POST /api/content.
// Keeping this file as a no-op placeholder to avoid accidental execution.

console.log(
  "seedAbout.js is deprecated — use POST /api/content to import data.",
);
process.exit(0);
